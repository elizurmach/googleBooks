'use strict';

describe('myBooks.book-list module', function() {

  beforeEach(module('myBooks.book-list'));

  describe('book-list controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var book-listCtrl = $controller('book-listCtrl');
      expect(book-listCtrl).toBeDefined();
    }));

  });
});